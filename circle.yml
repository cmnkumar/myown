version: 2

   docker:
      - image: buildpack-deps:trusty

dependencies:
  override:
    - sudo pip install awscli

test:
  override:

deployment:
  staging:
    branch: master
    commands:
    - echo "hello world"
 #   - ./deploy_staging.sh
