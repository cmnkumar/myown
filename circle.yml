  ruby:
    version:
      1.8.7-p358-falcon-perf

    dependencies: 
      override:
    - sudo pip install awscli

test:
  override:

deployment:
  staging:
    branch: master
    commands:
    - echo "hello world"
 #   - ./deploy_staging.sh
