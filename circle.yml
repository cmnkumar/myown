machine:
  node:
    version: 5.5.0
  environment:
    foo: bar1
 dependencies:
    override:
    - sudo pip install awscli

 test:
  override:
  
  deployment:
  staging:
    branch: master
    commands:
    - echo "hello world"
